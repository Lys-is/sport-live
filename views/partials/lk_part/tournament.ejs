<div class="team__wrapper">
    <h1 class="lk_wrapper__title">Команды</h1>
    <table class="tournament__table">
    <% if (tournament.length > 0) { %>
        <tr>
            <th>Название</th>
            <th>Дата создания</th>
            <th>Создатель</th>
            <th>Действия</th>
        </tr>
        <% for (let i = 0; i < tournament.length; i++) { %>
            <tr class="team__item">
                
                    <td class="team__item__title">
                        <%= tournament[i].name %>
                    </td>
                    <td class="team__item__date">
                        <%= tournament[i].date %>
                    </td>
                    <td class="team__item__creator">
                        <%= tournament[i].creator.email %>
                    </td>
                    <td class="team__item__edit">
                        <span class="nav_link" data-href="team/admins/<%= tournament[i]._id %>" class="team__item__edit__link">Админы</span>
                        <span class="nav_link" data-href="team/list<%= tournament[i]._id %>" class="team__item__edit__link">Состав</span>
                        <span class="nav_link" data-href="team/representative/<%= tournament[i]._id %>" class="team__item__edit__link">Руководитель</span>
                    </td>
            </tr>
        
        <% } %>
    </table>
    <%} else { %>
        <h1 class="lk_wrapper__title">Нет команд</h1>
    <% } %>
    <script>
        
    </script>
</div>